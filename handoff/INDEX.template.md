<!-- 交接文档模板。复制到 .handoff/<MMDD-HHMM>-<slug>/INDEX.md 后填写。
     硬上限：≤70 行、≤1.5k tokens（要超先砍"历史过程"，绝不砍硬约束/验证命令/证据路径/状态标记）。
     禁止贴代码/日志/原文，只给路径。
     §2/§3/§5/§6 里的每条判断句都要标 [有效] / [存疑] / [已否证] / [受限于环境]。
     [有效] 必须附**证据**路径（输出文件/测试结果/日志），不能附脚本或源码路径。
     [受限于环境] = 断言本身没错，但继任会话**复现不了**（缺子进程/缺网络/缺图形界面/缺真机）。
     凡断言一律是**快照**：继任者重跑命令时环境差异不算回归，要按 §6 的环境限制判读。 -->

# 交接：<一句话标题>

- **时间**：<YYYY-MM-DD HH:MM> ｜ **本目录**：`<绝对路径>`
- **上一个会话**：`<session id>`「<标题>」（起于 <时间>，<N> 次请求，最大上下文 <N>k）
- **继任**：新会话档位 `off`（执行型）/ `high`（决策型）→ 说 `读 <本文件绝对路径>，按它继续`
- **读完先汇报概况，再问我"这轮做什么"** —— `§4 下一步` 是**候选**，不是派工单；未经我决定不得开工
- **检索预算**：本轮 ≤15 次工具调用、网页 ≤2 次；超预算先问我
- **档位提示**：若开局（<30k）判断以执行为主，先切 `off` 再动手；`max` 只在难题时、只在新会话里开


## 1. 目标

<1–2 句，可验证。说清"做完什么算完成"。
本文件**不派活**：只说清局面，开工与否由继任者问过我之后定。

## 2. 已完成（结果 → 路径）

- [有效] <结果一句话> → `<绝对路径>`
- [已否证] <曾经相信、后来被推翻的结论：错在哪、为什么错>

## 3. 未决

- <现在到底卡在哪，1–3 条>
- [存疑] <推断但未验证的前提>

## 4. 下一步（候选，1–3 条 —— **等继任者问过我、我点头才做**）

每条格式：**动作 → 探活命令（期望输出）→ 探活失败怎么办**。
依赖外部主机/网络/真机的步骤，**没写探活命令与备用路径就等于没写**（继任者只能空等超时）。

1. <候选动作> ｜ 探活：`<<10 秒内可跑完的命令>`（期望：`<输出/退出码>`）｜ 失败：`<降级路径 / 或"停下来问我，别硬试">`
2. <候选动作> ｜ 探活：`<命令>`（期望：`<值>`）｜ 失败：`<降级路径>`

## 5. 硬约束

- [有效] <不能碰的东西 / 已确认的事实>
- [存疑] <推断但未验证 —— 继任者不得据此继续推导>

## 6. 验证方式

先写**执行环境限制**（继任者不写这个，"绿"就复现不了，他会以为东西坏了）：

- <例：本会话沙箱禁止管道 → 任何在脚本内部 `spawn` 子进程的自检会报 EPERM/exit 2，
  那是环境不是缺陷；> 这条断言在受管环境里应记为 **[受限于环境]**，不要记成 [有效]，更不要记成 FAIL。
- <例：无网络出口 / 无 WSL / 无图形界面 / 无真机>
- **以上为快照**：`<命令>` 的输出是 <时间> 一次性观测值；继任会话重跑若与本文不符，
  先按本节判读，再动仓库。

做法与判据（**每条 [有效] 都要写明"是在哪个环境里绿的"**，换个环境就跑不出同样的绿）：

<怎么判断做对了：命令 + 期望输出/阈值，或人工检查点。>

## 7. 需要时去哪找（不给内容，只给指针）

- 权威状态：`<路径>`
- 相关报告：`.handoff/reports/<文件>`
- 上一个交接：`.handoff/<目录>/INDEX.md`
